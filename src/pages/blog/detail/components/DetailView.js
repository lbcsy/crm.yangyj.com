import { PureComponent } from 'react';

export default class DetailForm extends PureComponent {
    render() {
        return (
            <div>
                视图组件
            </div>
        );
    };
};